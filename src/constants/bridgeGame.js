const GAME_COMMAND = {
  up: 'U',
  down: 'D',
  retry: 'R',
  quit: 'Q',
};

const GAME_INFO = {
  minSize: 3,
  maxSize: 20,
};

const GAME_PATTERN = {
  o: 'O',
  x: 'X',
  vertical: '|',
};

module.exports = {
  GAME_COMMAND,
  GAME_INFO,
  GAME_PATTERN,
};
